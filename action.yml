name: draw.io CLI
author: SevergroupTT
description: 'Run drawio-desktop'

inputs:
  source:
    description: 'Input file/drectory name'
    required: true
  destination:
    destination: 'Output file/directory name'
    required: true
  format:
    description: 'Ouput file format'
    required: false
    default: 'svg'

runs:
  using: docker
  image: Dockerfile

